import{a as l,c as i,b as a,o}from"./app-C1_q1qKw.js";const s={};function t(p,e){return o(),i("div",null,e[0]||(e[0]=[a('<blockquote></blockquote><h2 id="持久化" tabindex="-1"><a class="header-anchor" href="#持久化"><span>持久化</span></a></h2><blockquote><p>redis有两种持久化方式，分别为</p></blockquote><ul><li>RDB[Redis DataBase]</li><li>AOF[Append Only File]</li></ul><h3 id="rdb【redis-database】方式" tabindex="-1"><a class="header-anchor" href="#rdb【redis-database】方式"><span>rdb【Redis DataBase】方式</span></a></h3><blockquote><p>在指定的时间间隔内将内存中的数据集快照写入磁盘，也就是行话讲的snapshot快照，它恢复时是将快照文件直接读到内存里。</p></blockquote><p>RDB是怎么做的？</p><blockquote><p>Redis会单独创建(fork)一个子进程来进行持久化，会先将数据写入到一个临时文件中，待持久化过程都结束了，再用这个临时文件替换上次持久化好的文件。整个过程中，主进程是不进行任何IO操作的，这就确保了极高的性能。如果需要进行大规模数据的恢复，且对于数据恢复的完整性不是非常敏感，那RDB方式要比AOF方式更加的高效。RDB的缺点是最后一次持久化后的数据可能丢失。</p></blockquote><p>RDB的优势</p><ul><li>适合大规模的数据恢复</li><li>对数据完整性和一致性要求不高</li></ul><p>RDB的劣势</p><ul><li>在一定间隔时间做一次备份，所以如果redis意外down掉的话，就会丢失最后一次快照后的所有修改</li><li>FORK的时候，内存中的数据被克隆了一份，大致2倍的膨胀性需要考虑。</li></ul><h3 id="aof" tabindex="-1"><a class="header-anchor" href="#aof"><span>aof</span></a></h3><blockquote><p>以日志的形式来记录每个写操作，将Redis执行过的所有写指令记录下来(读操作不记录),只许追加文件但不可以改写文件，Redis启动之初会读取该文件重新构建数据，换言之，redis重启的话就根据日志文件的内容将写指令从前到后执行一次以完成数据的恢复工作。</p></blockquote><p>1.AOF的优点</p><ul><li>每修改同步：appendfsync always，同步持久化，每次发生数据变更会被立即记录到磁盘，性能较差但数据完整性比较好。</li><li>每秒同步：appendfsync everysec, 异步操作，每秒记录，如果一稍内宕机，有数据丢失。</li><li>不同步：appendfsync no，从不同步</li></ul><p>2.AOP的缺点</p><ul><li>相同数据集的数据而言aof文件要远大于rdb文件，恢复速度慢于rdb</li><li>aof运行效率要慢于rdb，每秒同步策略效率较好。</li></ul><p>注： AOF和RDB是可以共存的，当即存在dump.rdb文件，也存在appendonly.aof文件时，我们启动redis-server时，它会优先读取appendonly.aof文件，因为这个文件可以更好地保证数据的稳定性。</p><p>如果因为网络问题或是丢包等事情造成appendonly.aof文件损坏，导致redis-server启不了，则可以执行如下命令来恢复appendonly.aof文件</p><div class="language-shell line-numbers-mode" data-highlighter="shiki" data-ext="shell" style="--shiki-light:#393a34;--shiki-dark:#dbd7caee;--shiki-light-bg:#ffffff;--shiki-dark-bg:#121212;"><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code"><code class="language-shell"><span class="line"><span style="--shiki-light:#59873A;--shiki-dark:#80A665;">redis-check-aof</span><span style="--shiki-light:#A65E2B;--shiki-dark:#C99076;"> --fix</span><span style="--shiki-light:#B56959;--shiki-dark:#C98A7D;"> appendonly.aof</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>此命令会把所有不符合appendonly.aof文件格式的内容全部截掉，以保证aof文件合格。</p><ol start="3"><li>AOF重写[rewrite]</li></ol><blockquote><p>是什么？</p></blockquote><blockquote><p>重写原理：AOF文件持续增长而过大时，会fork出一条新进程来将文件重写(也是先写临时文件最后再rename),遍历新进程的内存中数据，每条记录有一条的Set语句。重写AOF文件的操作，并没有读取旧的aof文件，而是将整个内存中的数据库内容用命令的方式重写了一个新的aof文件，这点和快照有点类似</p></blockquote><blockquote><p>触发机制：Redis会记录上次重写时的AOF文件大小，默认配置是当AOF文件大小是上次rewrite后大小的一倍且文件大于64M时触发。【一般大型公司这个值会改成3G左右大小】</p></blockquote><blockquote><p>相关的配置项</p><ul><li>appendonly //改为 yes 表示启用 aof机制</li><li>appendfilename //指定生成的 aof文件名</li><li>appendfsync //异步</li><li>no-appendfsync-on-rewrite //重写时是否可以运用appendfsync,用默认no即可，保数据安全性</li><li>auto-aof-rewrite-min-size //设置重写的基准值，默认是64M</li><li>auto-aof-rewrite-percentage //设置重写的基准值，默认是100</li></ul></blockquote><h3 id="rdb-和-aof使用建议" tabindex="-1"><a class="header-anchor" href="#rdb-和-aof使用建议"><span>RDB 和 AOF使用建议</span></a></h3><ol><li><p>如果是只做缓存的话，可以不使用任何的持久化方式，也就是关掉RDB和AOF</p></li><li><p>如果对数据的一致性要求不高时，就可以直接使用RDB，它是最简单的持久化策略</p></li><li><p>如果同时开启2种持久化方式的话，当redis重启的时候会优先载入AOF文件来恢复原始的数据，因为在通常情况下AOF文件保存的数据集要比RDB文件保存的数据集要完整。RDB的数据不实时，同时使用两者时服务器重启也只会找AOF文件。那要不要只使用AOF呢？作者建议不要，因为RDB更适合用于备份数据库(AOF在不断变化，不好备份)，快速重启，而且不会有AOF可能潜在的BUG，留着作为一个万一的手段。</p></li></ol><h3 id="性能建议" tabindex="-1"><a class="header-anchor" href="#性能建议"><span>性能建议</span></a></h3><blockquote><p>因为RDB文件只用作后备用途，建议只在slave上持久化RDB文件，而且只要15分钟备份一次就够了，只保留<code>save 900 1</code>这条规则。</p></blockquote><blockquote><p>如果Enable AOF，好处是在最恶劣情况下也只会丢失不超过2秒数据，启动脚本较简单只load自己的AOF文件就可以了。代价一是带来了持续的IO，二是AOF rewrite的最后将rewrite过程中产生的新数据写到新文件造成的阻塞几乎是不可避免的。只要硬盘许可，应该尽量减少AOF rewrite的频率，AOF重写的基础大小默认值64M太小了，可以设到5G以上。默认超过原大小100%大小时重写可以改到适当的数值。</p></blockquote><blockquote><p>如果不Enable AOF，仅靠Master-Slave Replication实现高可用性也可以。能省掉一大笔IO也减少了rewrite时带来的系统波动。代价是如果Master/Slave同时倒掉，会丢失十几分钟的数据，启动脚本也要比较两个Master/Slave中的RDB文件，载入较新的那个。【新浪微博就选用了这种架构】</p></blockquote><h2 id="redis事务" tabindex="-1"><a class="header-anchor" href="#redis事务"><span>Redis事务</span></a></h2><ol><li>定义</li></ol><blockquote><p>可以一次执行多个命令，本质是一组命令的集合。一个事务中的所有命令都会被序列化。按顺序地串行化执行而不会被其它命令插入，不许加塞.</p></blockquote><ol start="2"><li>事务能干什么？</li></ol><blockquote><p>一个队列中，一次性、顺序性、排他性的执行一系列命令。</p></blockquote><ol start="3"><li>该如何使用?</li></ol><blockquote><p>常用命令</p></blockquote><ul><li>DISCARD 消取事务，放弃执行事务块内的所有命令</li><li>EXEC 执行所有事务块内的命令</li><li>MULTI 标记一个事务块的开始</li><li>UNWATCH 取消WATCH命令对所有 key的监视</li><li>WATCH key [key ...] 监视一个(或多个)key,如果在事务执行之前这个(或这些)key被其它命令所改动，那么事务将被打断</li></ul><p>注：redis支持部份事务，它有如下几种情况</p><ul><li>正常执行,就是加入事务的所有命令都是正确的，并且执行时也没有错误发生</li><li>放弃事务，就是加入事务的所有命令都是正确，并且可以执行的，但是，最后没有执行<code>EXEC</code>,而是执行了<code>DISCARD</code>，表示放弃此事务</li><li>全体连坐，是指加入事务的命令中，其中有任何一条命令语法是错误的，则再加入时，REDIS就会报错，那本次事务的所有命令都会撤消。</li><li>冤有头债有主，是指加入事务的命令中，语法都没有错误，但是其中有任何一条或多条存在运行时错误【比如字符串值做了自增】,则这种情况下，REDIS不会回滚事务中的所有命令，而只是运行时错误的命令不执行，其它命令正常执行，这也就是所谓的部份支持事务。</li><li>watch监控，监视一个或多个key,如果这个key被别的客户改变了，则在事务提交时会执行失败。</li></ul><p>小结：</p><blockquote><p>watch指令，类似于乐观锁，事务提交时，如果key的值已被另的客户改为，比如某个list已被别的客户端push/pop过了，整个事务队列都不会被执行。 通过watch命令在事务之前监控了多个key，倘若在watch之后有任何key的值发生了变化，EXEC命令执行的事务都将被放弃，同时返回Nullmulti-bulk应答通知调用者事务执行失败。</p></blockquote><ol start="4"><li>三个阶段</li></ol><ul><li>开启：以MULTI开始一个事务</li><li>入队：将多个命令入队到事务中，接到这些命令并不会立即执行，而是放到等待执行的事务队列里面</li><li>执行：由EXEC命令触发事务</li></ul><ol start="5"><li>三个特性</li></ol><ul><li>单独的隔离操作：事务中的所有命令都会序列化、按顺序执行。事务在执行的过程中，不会被其它客户端发送来的命令请求所打断。</li><li>没有隔离级别的概念：队列中的命令没有提交之前都不会实际的被执行，因为事务提交前任何指令都不会被实际执行，也就不存在“事务内的查询要看到事务的更新，在事务外查询不能看到”这个让人万分头痛的问题。</li><li>不保证原子性：redis同一个事务中如果有一条命令执行失败，其后的命令仍然会被执行，没有回滚。</li></ul>',49)]))}const r=l(s,[["render",t]]),n=JSON.parse('{"path":"/db/redis/aodsbod0/","title":"Redis的持久化和事务","lang":"zh-CN","frontmatter":{"title":"Redis的持久化和事务","createTime":"2025/08/12 16:30:42","permalink":"/db/redis/aodsbod0/"},"readingTime":{"minutes":8.08,"words":2425},"git":{"createdTime":1756113931000},"filePathRelative":"notes/db/redis/redis-03.md","headers":[]}');export{r as comp,n as data};
